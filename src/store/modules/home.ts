// import { LoginState, MenuItem, User, EnumItem, watermark } from '../interfaces/home.interface';
import { VuexArgs } from '../interfaces/common.interface';
import http from '@/core/http.service';
import core from '@/core/core.service';
import { AxiosResponse } from 'axios';

export const home = {

  getters: {
    // 传入枚举值类型code，获取相对应的枚举值


    //判断是否有某个权限

  },
  mutations: {

  },
  actions: {




  }
}
